<% flash.each do |type,msg| %>
    <div class="ui message <%= type == 'success' ? 'success' : 'negative' %>">
        <i class="close icon"></i>
        <div class="header">
            <%= msg %>
        </div>
    </div>
<% end %>